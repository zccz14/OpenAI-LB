# Product

## Register

product

## Users

产品服务两类用户：

- 管理员负责注册和维护 CodeX OAuth 上游提供商，观察容量、限流、故障、自动禁用与恢复状态，并审计逐次代理调用。
- 租户用户通过 Auth Mini 注册和登录，为每个 AI App 创建、吊销独立的 Consumer，并按 Consumer 查看调用量、成功率和错误记录。

两类用户都处于明确的运维或集成任务中；界面必须让当前租户、Consumer、上游提供商和请求上下文始终清晰。

## Product Purpose

本产品是一个以 Rust 编写、以 SQLite 持久化、最终可作为单一 Binary 部署的 CodeX OAuth 反向代理与负载均衡系统。管理员可使用多组 `(access_key, refresh_key)` 注册上游提供商；系统只代理 OpenAI / CodeX 能力，不兼容其他 AI 厂商协议，同时覆盖文本、推理、实时、语音、图像及后续 OpenAI 模态，而非绑定某一种 LLM 请求形态。

成功意味着：租户能用自有 Consumer 稳定访问代理；系统能按 Consumer 统计用量、为每次 API Call 留下基础审计记录；调度具备上游提供商亲和性；上游提供商 Rate Limit 可被跟踪，并能在受限或异常时自动禁用、在条件满足后自动恢复；管理员能从 UI 快速判断当前服务状态并采取行动。

## Brand Personality

冷静、精确、运维导向。产品应像可信赖的基础设施控制台：信息密度高但层级清楚，反馈直接，不使用营销语言掩盖系统状态。默认提供简体中文与英文，并保持术语一致。

## Anti-references

避免营销型大屏、超大 Hero、霓虹或 AI 渐变、玻璃拟态、没有行动价值的装饰图表、重复的同尺寸卡片阵列，以及通用 SaaS 落地页模式。不要为了“科技感”隐藏原始状态、时间、错误原因或上游提供商上下文。

## Design Principles

1. 先显示可行动的实时系统状态，并在每个操作和数据视图中明确租户、Consumer、上游提供商与时间范围。
2. 密集数据优先使用可扫描、可筛选的表格；卡片只用于真正独立的对象或摘要。
3. 故障、限流、禁用与恢复必须给出原因、时间和下一步，而不只给颜色。
4. 凭据默认不可见；只展示完成任务所需的脱敏元数据，并记录敏感管理操作。
5. 使用 shadcn/ui 的标准组件语义和交互状态，保持跨页面一致，不创造陌生控件。

## Accessibility & Inclusion

目标为 WCAG 2.1 AA。所有导航、表单、表格操作和弹层必须支持键盘；焦点始终可见；状态不能只依赖颜色；正文与占位文本满足对比度要求；表格在窄屏保留上下文；所有非必要动画尊重 `prefers-reduced-motion`。简体中文与英文界面须允许文案增长，不以固定宽度截断关键信息。
